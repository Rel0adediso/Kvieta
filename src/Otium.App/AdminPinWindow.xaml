<Window x:Class="Otium.App.AdminPinWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Otium · Yönetici PIN'i" Width="390" Height="330"
        WindowStyle="None" ResizeMode="NoResize" WindowStartupLocation="CenterOwner"
        Background="Transparent" AllowsTransparency="True" ShowInTaskbar="False"
        KeyDown="Window_KeyDown" Loaded="Window_Loaded">
    <Window.Resources>
        <Style TargetType="PasswordBox">
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}" />
            <Setter Property="Background" Value="{DynamicResource InputBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="CaretBrush" Value="{DynamicResource PrimaryBrush}" />
        </Style>
    </Window.Resources>
    <Border x:Name="DialogSurface" Background="{DynamicResource SurfaceRaisedBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="12">
        <Grid>
            <Grid.RowDefinitions><RowDefinition Height="38" /><RowDefinition Height="*" /></Grid.RowDefinitions>
            <Border BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,0,0,1" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Grid Margin="14,0"><TextBlock Text="Otium" FontSize="12" FontWeight="SemiBold" VerticalAlignment="Center" /><Button Content="×" Width="26" Height="26" Padding="0" HorizontalAlignment="Right" VerticalAlignment="Center" Style="{StaticResource SecondaryButtonStyle}" Click="Cancel_Click" /></Grid>
            </Border>

            <Grid Grid.Row="1" Margin="26,22,26,24">
                <Grid.RowDefinitions><RowDefinition Height="Auto" /><RowDefinition Height="Auto" /><RowDefinition Height="Auto" /><RowDefinition Height="Auto" /><RowDefinition Height="*" /><RowDefinition Height="Auto" /></Grid.RowDefinitions>
                <TextBlock x:Name="TitleText" FontSize="21" FontWeight="SemiBold" />
                <TextBlock x:Name="DescriptionText" Grid.Row="1" Foreground="{DynamicResource MutedTextBrush}" FontSize="12" TextWrapping="Wrap" Margin="0,6,0,17" />

                <StackPanel Grid.Row="2">
                    <TextBlock Text="PIN" Foreground="{DynamicResource MutedTextBrush}" FontSize="11" Margin="0,0,0,6" />
                    <PasswordBox x:Name="PinBox" Height="36" MaxLength="8" FontSize="17" PasswordChar="●" Padding="11,5" PreviewTextInput="PinBox_PreviewTextInput" />
                </StackPanel>

                <StackPanel x:Name="ConfirmPanel" Grid.Row="3" Margin="0,12,0,0">
                    <TextBlock Text="{DynamicResource PinRepeat}" Foreground="{DynamicResource MutedTextBrush}" FontSize="11" Margin="0,0,0,6" />
                    <PasswordBox x:Name="ConfirmPinBox" Height="36" MaxLength="8" FontSize="17" PasswordChar="●" Padding="11,5" PreviewTextInput="PinBox_PreviewTextInput" />
                </StackPanel>

                <TextBlock x:Name="ErrorText" Grid.Row="4" Foreground="{DynamicResource DangerBrush}" FontSize="11" TextWrapping="Wrap" Margin="0,10,0,0" />
                <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="{DynamicResource Cancel}" Style="{StaticResource SecondaryButtonStyle}" Click="Cancel_Click" Margin="0,0,8,0" />
                    <Button x:Name="ConfirmButton" Content="{DynamicResource Continue}" Style="{StaticResource PrimaryButtonStyle}" Click="Confirm_Click" MinWidth="92" />
                </StackPanel>
            </Grid>
        </Grid>
    </Border>
</Window>
