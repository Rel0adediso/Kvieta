<Window x:Class="Otium.App.ApplicationUsageDetailsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Width="520" Height="560" MinWidth="440" MinHeight="420"
        WindowStartupLocation="CenterOwner" WindowStyle="None"
        AllowsTransparency="True" Background="Transparent" ResizeMode="CanResizeWithGrip">
    <Border Background="{DynamicResource BackgroundBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="14" Padding="22">
        <Grid>
            <Grid.RowDefinitions><RowDefinition Height="Auto" /><RowDefinition Height="Auto" /><RowDefinition Height="*" /></Grid.RowDefinitions>
            <Grid MouseLeftButtonDown="Header_MouseLeftButtonDown">
                <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
                <StackPanel><TextBlock Text="{DynamicResource HistoryApplicationDetails}" FontSize="20" FontWeight="SemiBold" /><TextBlock Text="{DynamicResource HistoryApplicationDetailsDescription}" Foreground="{DynamicResource MutedTextBrush}" FontSize="11" Margin="0,5,18,0" TextWrapping="Wrap" /></StackPanel>
                <Button Grid.Column="1" Content="×" Width="32" Height="32" Padding="0" Style="{StaticResource SecondaryButtonStyle}" Click="Close_Click" />
            </Grid>
            <Border Grid.Row="1" Height="1" Background="{DynamicResource BorderBrush}" Margin="0,18,0,10" />
            <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                <ItemsControl x:Name="ApplicationsList">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Grid Height="58">
                                <Grid.ColumnDefinitions><ColumnDefinition Width="42" /><ColumnDefinition Width="*" /><ColumnDefinition Width="84" /></Grid.ColumnDefinitions>
                                <TextBlock Text="{Binding RankText}" Foreground="{DynamicResource FaintTextBrush}" FontSize="11" FontWeight="SemiBold" VerticalAlignment="Center" />
                                <StackPanel Grid.Column="1" VerticalAlignment="Center"><TextBlock Text="{Binding Name}" FontSize="12" FontWeight="SemiBold" TextTrimming="CharacterEllipsis" /><ProgressBar Value="{Binding RelativePercent}" Maximum="100" Height="4" Margin="0,8,18,0" /></StackPanel>
                                <TextBlock Grid.Column="2" Text="{Binding UsedText}" Foreground="{DynamicResource MutedTextBrush}" FontSize="11" FontWeight="SemiBold" HorizontalAlignment="Right" VerticalAlignment="Center" />
                            </Grid>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Grid>
    </Border>
</Window>
